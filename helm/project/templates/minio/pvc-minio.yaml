apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: "{{ include "etandem.name" . }}-pvc-{{ .Values.minio.name }}"
  annotations:
    "helm.sh/resource-policy": keep
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: "{{ .Values.minio.pv.storage }}"
  storageClassName: "{{ .Values.minio.pv.storageClassName }}"