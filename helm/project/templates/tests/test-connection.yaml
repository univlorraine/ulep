apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "etandem.fullname" . }}-test-connection"
  labels:
    {{- include "etandem.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "etandem.fullname" . }}:{{ .Values.api.service.port }}']
  restartPolicy: Never
