apiVersion: v1
kind: Service
metadata:
  name: "{{ .Values.name }}-{{ .Values.auth.name }}"
  labels:
    {{- include "etandem.labels" . | nindent 4 }}
spec:
  type: {{ .Values.auth.service.type }}
  ports:
    - port: {{ .Values.auth.service.port }}
      targetPort: direct
      protocol: TCP
      name: direct
  selector:
    {{- include "etandem.selectorLabels" . | nindent 4 }}
