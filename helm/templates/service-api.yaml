apiVersion: v1
kind: Service
metadata:
  name: "{{ .Values.name }}-{{ .Values.api.name }}"
  labels:
    {{- include "etandem.labels" . | nindent 4 }}
spec:
  type: {{ .Values.api.service.type }}
  ports:
    - port: {{ .Values.api.service.port }}
      targetPort: direct
      protocol: TCP
      name: direct
  selector:
    {{- include "etandem.selectorLabels" . | nindent 4 }}
