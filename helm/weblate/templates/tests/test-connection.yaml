apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "etandem-weblate.fullname" . }}-test-connection"
  labels:
    {{- include "etandem-weblate.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "etandem-weblate.fullname" . }}:{{ .Values.weblate.service.port }}']
  restartPolicy: Never
