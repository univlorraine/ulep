apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ulep-weblate.fullname" . }}-test-connection"
  labels:
    {{- include "ulep-weblate.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ulep-weblate.fullname" . }}:{{ .Values.weblate.service.port }}']
  restartPolicy: Never
